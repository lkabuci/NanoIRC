#include "JOIN.hpp"

// JOIN::JOIN() {}

// JOIN::JOIN(const JOIN& join) : _channels(join._channels), _keys(join._keys)
// {}

// JOIN::~JOIN() {}

// JOIN&   JOIN::operator=(const JOIN& join) {
//     if (this == &join)
//         return *this;
//     _channels = join._channels;
//     _keys = join._keys;
//     return *this;
// }

// void    JOIN::execute(const std::string& first,
//                     const std::vector<std::string>& parameters)
//{
//     if (parameters.empty()) {
//         ErrorReplies::reply(2, "localhost", ERROR_CODES::ERR_NEEDMOREPARAMS,
//                             "?");
//         throw std::exception();
//     }
//     //_tokens = parameters;
//     channel();

//}

// void    JOIN::channel() {
// }
